import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.m2tectiq.krishnasales',
  appName: 'krishnasales',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
